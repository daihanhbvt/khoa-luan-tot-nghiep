export enum GetAction {
    GetFromDB = 'Get from Database',
}
export enum GetAllAction {
    GetFromDB = 'Get from Database',
}
export enum UpdateAction {
    ValidateRequest = 'Validate Request',
    CheckFromDB = 'Check from DB',
    UpdateValue = 'Update value',
}
export enum DeleteAction {
    CheckFromDB = 'Check from DB',
    UpdateDeleteFlag = 'Update Delete Flag',
}
export enum CreateAction {
    ValidateRequest = 'Validate Request',
    CheckFromDB = 'Check from DB',
    AddRecord = 'Add Record',
}
